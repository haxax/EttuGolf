using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class CheatSheet
{
    // To add scriptable menu object 
    // Inherit : ScriptableObject
    // [CreateAssetMenu(fileName = "file", menuName = "section/scriptName", order = 0)]

    // [AddComponentMenu("section/componentName")]
    // Hide from inspector menu: [AddComponentMenu("")]

    // [DisallowMultipleComponent]
    // [RequireComponent(typeof(Rigidbody))]

    // [Tooltip("", order = 1)]
    // [Header("")]
    // [Space(20)]
    // [Range(0,1)]

    // [HelpBox("", HelpBoxMessageType.None)]

    // [ScriptOrder(-500)]

    // ScriptableObject icon: Create a folder called "Gizmos". In it, add a texture - 128x128 works well - named "ClassName Icon.png".
}